{
  "name": "@track/api",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "bun build ./src/server.ts --outdir ./dist --target bun",
    "dev": "bun --watch ./src/server.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test test"
  },
  "dependencies": {
    "@track/core": "workspace:*",
    "@track/shared": "workspace:*",
    "hono": "^4.7.2"
  }
}
