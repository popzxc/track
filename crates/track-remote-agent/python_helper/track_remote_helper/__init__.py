# The remote helper is intentionally small and dependency-free. Keeping the
# package importable makes the zipapp entrypoint straightforward and lets local
# unit tests target the same modules the remote runtime uses.
