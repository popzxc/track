// Numbered migration test modules keep the historical fixture set visible and
// make it obvious which pre-migration shape each case exercises.
mod support;

mod id_000_drop_task_dispatches_fk_cascade;
